  angle_min_coord_mag = numpy.hypot(angle_and_min_coord_start.x, angle_and_min_coord_start.y)

  angle_min_x = angle_and_min_coord_start.x
  angle_min_y = angle_and_min_coord_start.y
  angle_min_coord_mag = numpy.hypot(angle_min_x, angle_min_y)
  start_dir_perp = numpy.asarray([-start_dir[1], start_dir[0]])

  xy_pairs = set()
  for longitudinal_attempt in range(1, 10):
    longitudinal_x = longitudinal_attempt * angle_min_x
    longitudinal_y = longitudinal_attempt * angle_min_y

    for lateral_attempt in range(-10, 10):
      x = longitudinal_x + start_dir_perp[0] * angle_min_coord_mag * lateral_attempt
      y = longitudinal_y + start_dir_perp[1] * angle_min_coord_mag * lateral_attempt
